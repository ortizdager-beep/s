{
  "name": "ops-form-app",
  "version": "1.0.0",
  "private": true,
  "description": "Prototipo fullstack: formulario dinámico para ops leaders con autenticación, carga desde Excel y envío por email.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "seed": "npm run seed --prefix server",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client"
  }
}
